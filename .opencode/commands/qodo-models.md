---
description: List available Qodo models
agent: qodo
---

List all available Qodo models:

Use `qodo_models` to get the complete list of available models.

If a provider is specified ($1), filter by that provider.

Present the models in a clear table format showing:
- Model ID
- Provider
- Context window size
- Supported features (images, PDF, thinking)
